{
  "name": "axiom-workspace-api",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute axiom-workspace --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute axiom-workspace --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "wrangler": "^3.99.0"
  }
}
